name: ChessClock
options:
  bundleIdPrefix: com.alek6dev
  deploymentTarget:
    iOS: "16.0"
  developmentLanguage: fr
settings:
  MARKETING_VERSION: "1.0.0"
  CURRENT_PROJECT_VERSION: "1"
targets:
  ChessClock:
    type: application
    platform: iOS
    sources:
      - ChessClock
    info:
      path: ChessClock/Info.plist
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.alek6dev.chessclock
        SWIFT_VERSION: "5.0"
        TARGETED_DEVICE_FAMILY: "1"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
